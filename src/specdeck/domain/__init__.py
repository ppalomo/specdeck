"""The domain: what Specdeck knows, with nothing about how it finds it out.

Models and pure functions only. Nothing here imports another layer of the package, opens a
file or starts a process — which is what lets every rule in it be tested without a disk, a
port or the `openspec` binary.
"""
